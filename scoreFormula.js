let currentScore = 0;
let correctResponses = 0;
let totalResponses = 0;

let accuracy = correctResponses / totalResponses;

let response;

if (response === true) {
  score += 10;
}

const accuracyBonus = accuracy * correctResponses * 10;
const finalScore = currentScore + accuracyBonus;
